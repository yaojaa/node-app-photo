<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <!-- 上述3个meta标签*必须*放在最前面，任何其他内容都*必须*跟随其后！ -->
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/common.min.css">
    <link rel="stylesheet" href="/css/font-awesome.min.css">
    <link rel="stylesheet" href="/css/buttons.css">
    <link rel="stylesheet" href="/css/userspace.css?9988">
    <style>
        .nav-tabs {
            margin-top: 50px;
        }
        .user-list img{
            width: 100px;
            height: 100px;
        }
    </style>
</head>
<div class="g-head">
    <ul class="nav navbar-nav navbar-right">
        {{#if isLogin}}
            {{#if isSelf}}
            {{else}}
                <li>
                    <a href="/follow/{{userInfo.id}}" title="关注" class="start-btn"><span
                            class="glyphicon glyphicon-plus"></span>关注</a>
                </li>
            {{/if}}
        {{else}}
            <li>
                <a href="/login" title="登录" class="start-btn"><span class="glyphicon glyphicon-user"></span>会员登录</a>
            </li>
            <li>
                <a href="/signup" title="注册" class="start-btn"><span class="glyphicon glyphicon-edit"></span>注册</a>
            </li>
        {{/if}}
        <li>
            <a href="/" title="首页" class="start-btn"><span class="glyphicon glyphicon-camera"></span>风影网</a>
        </li>
    </ul>
</div>
<div class="u_banner">
    <div class="u_logo">
        <img src="{{userInfo.avatar}}" alt="">
    </div>
    <div class="u_badge">
        {{userInfo.nickname}}
    </div>
</div>
<div class="tab_wrapper">
    <!-- Nav tabs -->
    <ul class="nav nav-tabs" role="tablist">
        <li role="presentation" {{#eq type '0'}}class="active"{{/eq}}>
            <a href="/user/follow/{{userInfo._id}}/0">粉丝({{userInfo.fans.length}})</a>
        </li>
        <li role="presentation" {{#eq type '1'}}class="active"{{/eq}}>
            <a href="/user/follow/{{userInfo._id}}/1">关注({{userInfo.followings.length}})</a>
        </li>
    </ul>
    <!-- Tab panes -->
    <div class="tab-content" style="max-width:900px; margin:0 auto">
        <ul class="user-list">
            {{#each users}}
                <li>
                    <a href="/userspace/{{_id}}">
                        <img src="{{avatar}}" alt="">
                        <br>
                        <span class="color:#fff">{{nickname}}</span>
                    </a>
                </li>
            {{/each}}
        </ul>
    </div>
</div>

<div class="g-foot container" align="center">
    被访问{{userInfo.userspace_count}}次 | © {{userInfo.nickname}} | Powered by 风影网
</div>

<!-- jQuery文件。务必在bootstrap.min.js 之前引入 -->
<script src="//cdn.bootcss.com/jquery/1.11.3/jquery.min.js"></script>
<!-- 最新的 Bootstrap 核心 JavaScript 文件 -->
<script src="//cdn.bootcss.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
<script src="/js/common.js"></script>
